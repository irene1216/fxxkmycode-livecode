<!--pages/stories/stories.wxml-->
<view class="stories">
  <view class="subheader">Fxxk my code moments...</view>
  <view wx:for="{{stories}}" wx:for-item="story" data-index="{{index}}" data-testing="ice-cream" bindtap="tapOnCard" class="card {{activeIndex == index ? 'active' : ''}}">
    <view class="title">{{story.title}}</view>
    <view class="author">🗡{{story.author}}</view>
  </view>
</view>